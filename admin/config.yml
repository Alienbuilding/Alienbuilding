backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "pages"
    label: "Pages"
    files:
      - file: "index.html"
        name: "home"
        label: "Home Page"
        fields:
          - name: "title"
            label: "Title"
            widget: "string"
